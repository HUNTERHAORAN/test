name: Python CI with Fake Deploy

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run python script
        run: python test_numpy_pandas.py

  deploy:
      needs: ci
      runs-on: ubuntu-latest
      steps:
      - name: Fake deploy
        run: | 
          echo "🎉 Deploy stage: CI passed, deploying..."


#      - name: Check Python version
#        run: python --version